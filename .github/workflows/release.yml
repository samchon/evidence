name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  publish:
    name: Verify and publish
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout lint-plugin-evidence
        uses: actions/checkout@v4
        with:
          path: samchon/lint-plugin-evidence

      - name: Checkout ttsc
        uses: actions/checkout@v4
        with:
          repository: samchon/ttsc
          ref: v0.20.0
          path: samchon/ttsc

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.6.4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.x
          registry-url: https://registry.npmjs.org/
          cache: pnpm
          cache-dependency-path: samchon/lint-plugin-evidence/pnpm-lock.yaml

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: samchon/lint-plugin-evidence/packages/evidence/go.mod
          cache-dependency-path: samchon/lint-plugin-evidence/packages/evidence/go.sum

      - name: Install dependencies
        working-directory: samchon/lint-plugin-evidence
        run: pnpm install --frozen-lockfile

      - name: Publish to npm
        working-directory: samchon/lint-plugin-evidence
        run: pnpm run package:latest --no-git-checks --provenance

  release:
    name: Create GitHub release
    runs-on: ubuntu-latest
    needs: publish
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - name: Generate release notes
        run: pnpm dlx changelogithub
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
