{
  "name": "@samchon/evidence-test",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence logic unit tests.",
  "main": "src/index.ts",
  "scripts": {
    "build": "ttsc -p tsconfig.json --noEmit",
    "start": "ttsx -P tsconfig.json src/index.ts"
  },
  "dependencies": {
    "@samchon/evidence": "workspace:*",
    "@nestia/e2e": "catalog:samchon"
  },
  "devDependencies": {
    "@ttsc/lint": "catalog:typescript",
    "@types/node": "catalog:utils",
    "ttsc": "catalog:typescript",
    "typescript": "catalog:typescript"
  }
}
